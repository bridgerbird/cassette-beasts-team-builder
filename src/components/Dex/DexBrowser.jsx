export function DexBrowser() {
  return (
    <section className="panel">
      <h2>Dex Browser</h2>
      <p className="placeholder-note">
        Species list, filters, and sprites will load here once the
        Cassette Beasts data source is wired in.
      </p>
    </section>
  );
}
