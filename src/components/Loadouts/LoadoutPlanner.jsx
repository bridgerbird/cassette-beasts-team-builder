export function LoadoutPlanner() {
  return (
    <section className="panel">
      <h2>Loadout Planner</h2>
      <p className="placeholder-note">
        Per-monster move/sticker pool selection will live here, reading
        from and writing back to the current team.
      </p>
    </section>
  );
}
